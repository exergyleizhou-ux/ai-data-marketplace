# 绿洲 Oasis — 上线就绪清单

> **状态**: v7 本地工程全部完成（2026-06-10）。
> 本文档列出**只有用户/外部方能解锁的部署前置项**。代码侧全部就绪 —
> 每项都有接口/实现/配置入口,是「接真实例 / 换配置」而非「写代码」。

---

## 1. 支付 — 持牌方分账签约

| 列 | 内容 |
|---|------|
| **待办** | 微信支付 / 支付宝**持牌支付机构分账**签约 + 资金流向法务意见 |
| **谁来做** | 用户 + 持牌支付机构 (如汇付天下/通联支付) + 执业律师 |
| **代码已就绪** | `payment.PaymentProvider` 接口 (`backend/internal/modules/payment/provider.go:31`) + `SplitProvider` 接口 (line 42) + `RefundProvider` 接口 (line 50)。Stripe Connect 实现 (`stripe.go`) 作为参考 — 分账→结算→退款全链路已跑通(测试模式)。`MockProvider` (line 57) 用于沙箱 |
| **配置** | `PAYMENT_PROVIDER=wechat` 或 `alipay` + 商户凭证 (`config.go:36-40`) |
| **验证** | 沙箱单笔分账:买家付款 → 平台抽佣 + 卖家收款分开 → 确认资金到账 → 法务出具《二清合规意见函》 |
| **风险** | 🔴 **二清刑事红线** — 平台不得触碰买家资金。必须持牌机构做资金清分,平台只发分账指令 |

---

## 2. 部署 — ICP 备案 + EDI 许可 + 云资源

| 列 | 内容 |
|---|------|
| **待办** | 域名实名认证 → ICP 备案 → EDI 增值电信业务经营许可证 → 采购云资源(K8s 集群 / PG / Redis / 对象存储) |
| **谁来做** | 用户 |
| **代码已就绪** | `deploy/k8s/` 含完整 6 文件 manifest：namespace + config + migrate-job + backend + frontend + ingress。`deploy/docker-compose.prod.yml` 单机部署版。`backend/Dockerfile` 多阶段构建(golang:1.25 → distroless:nonroot) |
| **配置** | `APP_ENV=production` + k8s secrets / sealed-secrets 注入全部凭证 |
| **验证** | 真域名 `api.oasis.example.com` + `oasis.example.com` HTTPS 访问 → `curl https://api.oasis.example.com/api/v1/ping` 返回 `{"pong":true}` → 全链路可访问 |
| **风险** | 备案未通过则域名不可解析、不可上线。EDI 许可周期 3-6 个月,可先用「数据处理服务」类目起步 |

---

## 3. 邮件 — 真 SMTP 服务

| 列 | 内容 |
|---|------|
| **待办** | 接入真 SMTP 服务(SendGrid / 阿里云邮件推送 / AWS SES) |
| **谁来做** | 用户 |
| **代码已就绪** | `notification.EmailSender` 接口 (`backend/internal/modules/notification/email.go:16`) + `SMTPSender` 实现 (line 42) + `NewSMTPSender` 构造函数 (line 51)。已在 `server.go:306` 按 `SMTP_HOST` 是否存在自动接线 |
| **配置** | `SMTP_HOST` / `SMTP_PORT` / `SMTP_USER` / `SMTP_PASS` / `SMTP_FROM_ADDR` / `SMTP_FROM_NAME` |
| **验证** | 注册一个真实邮箱 → 检查收件箱收到 register 欢迎邮件 → 2FA 邮件 → 提现通知邮件 |
| **风险** | 新 IP/域名发件信誉低可能进垃圾箱,需配置 SPF/DKIM/DMARC。SendGrid 免费 tier 100 封/天够 MVP |

---

## 4. 对象存储 — 生产级

| 列 | 内容 |
|---|------|
| **待办** | 从本地文件存储切换到 S3 兼容对象存储(MinIO / AWS S3 / 阿里云 OSS) |
| **谁来做** | 用户 |
| **代码已就绪** | `storage.Storage` 接口 (`backend/internal/platform/storage/storage.go`) + `S3Driver` 已实现。`server.go:280` 按 `STORAGE_DRIVER` 选择驱动 |
| **配置** | `STORAGE_DRIVER=s3` + `S3_ENDPOINT` / `S3_BUCKET` / `S3_ACCESS_KEY` / `S3_SECRET_KEY` / `S3_REGION` |
| **验证** | 卖家通过分片上传一个测试文件 → 确认文件落在 S3 bucket → 买家下载成功 |
| **风险** | 非生产存储内容本地消失(PG 在,文件在 local dir,换 S3 后旧文件不可达)。先在空 bucket 验证 |

---

## 5. TEE (L2) — 机密计算云节点

| 列 | 内容 |
|---|------|
| **待办** | 采购 TDX/SEV 机密计算云节点 → 部署 DCAP 基础设施 → 部署远端 KBS |
| **谁来做** | 用户(开 TEE 云实例) |
| **代码已就绪** | `compute.Attester` 接口 (`backend/internal/modules/compute/runner_tee.go:48`) + `MockAttester` (line 58) + `TDXAttester` (`runner_tee_tdx.go:54`)。`server.go:432-453` 按 `COMPUTE_RUNNER` / `TEE_ATTESTER` / `KBS_URL` 选择链路。KBS 验证器 (`kbs.go:56`) 已实现 remote KBS 客户端 |
| **配置** | `COMPUTE_RUNNER=tee` / `TEE_ATTESTER=tdx` / `KBS_URL=https://kbs.oasis.example.com` |
| **验证** | 提交一个小型 mock 算法到数据集 → 远端 TDX 节点执行 → KBS 验证 attestation → 释放密钥 → 输出可见。`POST /api/v1/compute/jobs/:id/attestation` 返回真实 quote |
| **风险** | 国内 TEE 云(TDX)可用性有限。fallback 方案:`COMPUTE_RUNNER=docker`(L1 网络隔离)先上线,TDX 后补 |

---

## 6. MPC (L3) — 多方安全计算节点

| 列 | 内容 |
|---|------|
| **待办** | ≥2 个独立部署的 Secretflow/SPU 节点(不同组织/云账号) |
| **谁来做** | 用户 + 数据合作方 |
| **代码已就绪** | `compute.MPCOrchestrator` 接口 (`backend/internal/modules/compute/mpc.go:43`) + `mockMPC` 参考实现 (line 51)。`compute.Service` 已通过 `WithOrchestrator(o MPCOrchestrator)` 注入 (`service.go:73,111`) |
| **配置** | MPC 节点地址 + 双向 TLS 证书(由各方独立提供) |
| **验证** | 两方各持一个数据集 → 提交 PSI 求交 job → 双方节点执行 → 仅交集结果返回到平台 |
| **风险** | 多方协作调度复杂度高。先 L0(shadow mode 记录) + L1(docker 沙箱)上线,MPC 作为高价值增项 |

---

## 7. 法务 — ToS / 隐私政策 / 数据交易许可协议

| 列 | 内容 |
|---|------|
| **待办** | 执业律师定稿《用户服务协议》《隐私政策》《数据交易许可协议》 |
| **谁来做** | 用户 + 数据合规律师 |
| **代码已就绪** | `docs/legal/` 含 7 个文件：三份草案(`用户服务协议-草案.md` / `隐私政策-草案.md` / `数据交易许可协议-草案.md`) + 中英双语文档生成器(`generate-legal-docs.js`)。前端注册页(`register/page.tsx`)已注入协议勾选流程 |
| **配置** | 无代码配置 — 定稿后替换 `docs/legal/` 下的 markdown 文件,重跑 `node generate-legal-docs.js` 生成 `.docx` |
| **验证** | 律师出具《合规意见函》 → 注册页展示最终协议文本 → 用户必须勾选才能注册 |
| **风险** | 未经律师定稿的草案不可用于生产。PIPL 要求明确的用户告知 + 同意 + 撤回机制(已通过账户注销 + 数据导出实现) |

---

## 8. 存管 — 资金存管机构

| 列 | 内容 |
|---|------|
| **待办** | 选定资金存管银行/持牌机构 + 签订存管协议 |
| **谁来做** | 用户 |
| **代码已就绪** | `config.Config` 有预留占位项。`payment.SplitProvider` 接口在 provider 层天然支持分账到卖家和平台的不同账户 |
| **配置** | `CUSTODIAN_NAME` + 存管机构 API 凭证(待 provider 实现时定义) |
| **验证** | 存管协议签订 → 资金流向全额经过存管机构 → 平台不碰钱 |
| **风险** | 监管要求 — 无存管不可运营资金类业务。需在支付签约后立即启动 |

---

## 9. 数据库 — 生产 PG 实例

| 列 | 内容 |
|---|------|
| **待办** | 创建生产 PostgreSQL 16+ 实例(云厂商托管或自建) + 配置连接池 + 备份策略 |
| **谁来做** | 用户 |
| **代码已就绪** | `db.NewPool(cfg.DatabaseURL)` (`backend/internal/platform/db/db.go:17`) + 自动迁移 (`cmd/api/main.go:46-52`)。22 个迁移文件(`backend/migrations/`)已嵌入二进制 |
| **配置** | `DATABASE_URL=postgres://user:pass@host:5432/oasis?sslmode=require` + `AUTO_MIGRATE=true` |
| **验证** | 启动后端 → `AUTO_MIGRATE=true` 自动跑完 22 个迁移 → `SELECT count(*) FROM information_schema.tables WHERE table_schema='public'` 返回 ≥30 张表 |
| **风险** | 生产库务必启用 SSL + 强密码 + 每日 pg_dump + WAL 归档 |

---

## 10. Redis — 生产缓存/限流

| 列 | 内容 |
|---|------|
| **待办** | 创建生产 Redis 7+ 实例 |
| **谁来做** | 用户 |
| **代码已就绪** | `ratelimit.Limiter` 接口 (`backend/internal/platform/ratelimit/ratelimit.go:20`)。Redis 版(共享限流)已有实现;in-memory 版(single-instance fallback)自动降级 |
| **配置** | `REDIS_URL=redis://:password@host:6379/0` |
| **验证** | 后端启动日志无 "redis unavailable" 警告 → `redis-cli PING` → PONG |
| **风险** | Redis 不可用时自动降级到 in-memory 限流(单实例有效,多实例不共享)。生产务必配 Redis |

---

## 11. 监控 — Prometheus + Grafana + 告警

| 列 | 内容 |
|---|------|
| **待办** | 部署 Prometheus → 抓取 `/metrics` 端点 → Grafana 仪表板 → Alertmanager |
| **谁来做** | 用户 / SRE |
| **代码已就绪** | `/metrics` 端点已暴露 (`server.go:255`: `gin.WrapH(metrics.Handler())`)。`middleware.Metrics()` (line 121) 记录 HTTP 延迟分布。异常扫描器内建 Webhook 告警 (`anomaly/alerter.go`) |
| **配置** | `ANOMALY_WEBHOOK_URL` (钉钉/Slack webhook) + `ANOMALY_WEBHOOK_KINDS` |
| **验证** | Prometheus targets 显示 backend UP → Grafana 仪表板正常渲染 → 异常触发后 webhook 收到消息 |
| **风险** | 低 — 监控不影响核心功能,可后补 |

---

## 12. 域名 + HTTPS 证书

| 列 | 内容 |
|---|------|
| **待办** | 注册域名 → 配置 DNS A/CNAME 记录 → 申请/自动续签 TLS 证书(cert-manager 或 ALB) |
| **谁来做** | 用户 |
| **代码已就绪** | `deploy/k8s/50-ingress.yaml` 配置 ingress host + TLS。前端 React 构建时 `NEXT_PUBLIC_API_BASE` 指向生产域名 |
| **配置** | `CORS_ALLOW_ORIGIN=https://oasis.example.com` + `APP_BASE_URL=https://app.example.com` |
| **验证** | `curl -v https://api.oasis.example.com/api/v1/ping` → TLS 1.3 + 有效证书 |
| **风险** | 证书过期 = 全站不可用。务必用 cert-manager 自动续签 |

---

## 未来扩展非门控(不阻塞上线, backlog)

| 方向 | 说明 | 代码切入点 |
|------|------|-----------|
| **语义搜索** | 当前是 PostgreSQL full-text search (`search_vector` 列)。可升级到 pgvector embedding + RAG | `internal/modules/search/handler.go` |
| **推荐系统** | 基于购买历史 / 浏览行为的协同过滤 | 需要新模块 `recommendation/` |
| **Hadoop / 数据湖** | 支持 HDFS / S3 数据源(`compute.datasetAdapter`) | `internal/modules/compute/adapter_dataset.go` |
| **多语言** | 当前前端中文,需 i18n 框架 | `frontend/messages/` (预留) |
| **移动端** | React Native / Flutter 壳 | 全部 API 已通过 OpenAPI spec 描述(`api/openapi.yaml`) |
| **OAuth / SSO** | 企业微信 / 飞书登录 | `auth.Register` 可加 provider |
| **数据交易市场联盟** | 跨平台数据集发现标准(W3C DCAT) | `dataset/croissant` 已输出 JSON-LD |

---

## 附录:本地验证命令(部署前最后自查)

```bash
# 后端全量测试
cd backend
T=$(mktemp -d); SOCK=$(mktemp -d); PORT=55400
initdb -D "$T" -U postgres --auth=trust >/dev/null
pg_ctl -D "$T" -o "-p $PORT -k $SOCK -c listen_addresses=''" -w start >/dev/null
DATABASE_URL="postgres://postgres@/postgres?host=$SOCK&port=$PORT&sslmode=disable" \
  go test -race -p 1 -count=1 ./...
pg_ctl -D "$T" stop -m fast >/dev/null
rm -rf "$T" "$SOCK"

# 前端构建验证
cd ../frontend
npm run typecheck && npm run lint && npm run build

# Docker 构建验证
cd ../backend
docker build -t oasis-backend:test .
```

---

> **v7 本地工程终点**: 16 后端模块,22 迁移,121 API 端点(OpenAPI 3.0 文档化),C2D L0/L1/L2/L3,ops 看板,
> 通知(邮件+站内),合规(PIPL 导出+注销),2FA TOTP+密码重置,异常告警,审计日志,提现审批,
> Q&A,收藏,全栈 E2E 测试,安全不变量测试,handler 集成测试。**代码就绪,只差外部门控。**
