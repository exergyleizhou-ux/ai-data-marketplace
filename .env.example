# Copy to .env and adjust. NEVER commit a real .env.
# docker-compose.yml sets these for the containerized stack; this file is for
# running backend/frontend directly on the host.

# --- backend ---
APP_ENV=development
HTTP_ADDR=:8080
DATABASE_URL=postgres://app:app@localhost:5432/ai_data_marketplace?sslmode=disable
REDIS_URL=redis://localhost:6379/0

# --- frontend ---
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080/api/v1

# --- external services (wired in later PRs; keep secrets out of git) ---
# OSS_ACCESS_KEY_ID=
# OSS_ACCESS_KEY_SECRET=
# OSS_BUCKET=
# OSS_ENDPOINT=
# JWT_SECRET=
# PAYMENT_PROVIDER=          # licensed aggregator (see docs §2.1)
# PAYMENT_MCH_ID=
# PAYMENT_API_KEY=
